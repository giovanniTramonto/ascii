{
  "name": "ascii",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "^23.0.0",
    "yarn": "^4.5.1"
  },
  "packageManager": "yarn@4.5.1",
  "scripts": {
    "dev": "yarn workspaces foreach --all --parallel --interlaced run dev"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}
